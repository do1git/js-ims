nav 

    a.header__logo(href="/")
        img(src="/static/statics/gnmLogo.png") 
    
    div.header__menu
        a.header__menu__plan(href="/")
            .header__memu__plan__box 
                i.fas.fa-clipboard-list
        a.header__menu__done(href="/done")
            .header__memu__done__box 
                i.fas.fa-clipboard-check  
        a.header__menu__manage(href="/manage")
            .header__memu__manage__box 
                i.fas.fa-edit
        a.header__menu__menu(href="/etc")
            .header__memu__menu__box 
                i.fas.fa-bars     
    
    div.header__user 
        a(href=`/users/${loggedInUser._id}`)
            if (loggedInUser.avatar==null)
                img(src="/static/statics/defaultProfile.jpeg",width="30px")
            else
                img(src=`/${loggedInUser.avatar}`,width="30px")