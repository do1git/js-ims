extends base
include mixins/planMixin

block content 
    .houtbound 
        if plans_k
            .houtbound__list
                each plan in plans_k
                    +plan(plan)
        else 
            div.no__info.searchPlanDetail__no__info 
                i.fas.fa-exclamation-triangle
                span 해당 기간의 일정이 없습니다

    script(src="/static/dynamics/js/goToDetailSearch.js") 